version: '3.8'

services:
  chesterton-microservice:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    networks:
      - chesterton-network

networks:
  chesterton-network:
    driver: bridge 